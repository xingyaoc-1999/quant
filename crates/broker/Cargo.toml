[package]
name = "broker"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
binance-sdk = { version = "49.0.0", features = [
    "spot",
    "derivatives_trading_usds_futures",
    "rustls-tls",
], default-features = false }

common = { path = "../common" }
quant = { path = "../quant" }
